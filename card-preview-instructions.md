# Task: create a set of preview pages

# Purpose
To be able to review and tweak the card texts and layout I need to see the playing cards on screen.

- Per protocol: Create a page with all playing cards are layed out, in the three visibility modes.
  - Zoomed card
  - In hand card
  - played in line card
  - (We do not reallly need the face -down variant as those are the same for all protocols)

- Make sure to use the same rendering code, so any changes will affect the game rendering as well.
- Use the correct protocol colors

- Present these pages as the main content of a table, with a index of all pages (in alphabetic order) as a left side column

- Propose a suitable location to store this.